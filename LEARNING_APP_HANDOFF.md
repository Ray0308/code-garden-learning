# モフリスのコードガーデン — 開発引き継ぎ書

更新日: 2026-07-21

## 1. 最初に確認するもの

- GitHub: https://github.com/Ray0308/code-garden-learning
- 公開版: https://ray0308.github.io/code-garden-learning/
- 既知の基準コミット: `eba164e`（一般学習版の初期コミット）
- 既定ブランチ: `main`

別端末では、まず次のコマンドで取得する。

```powershell
git clone https://github.com/Ray0308/code-garden-learning.git
cd code-garden-learning
node tests/levels.test.cjs
node --check app.js
node --check levels.js
```

## 2. 絶対に守る境界

このリポジトリは一般学習用の専用リポジトリである。

- 体験入社用は `Ray0308/code-garden-training` で別に開発する。
- 初期の体験研修版 `Ray0308/code-garden` も一般学習版の保存先には使わない。
- 一般学習版の作業で、体験入社版のリポジトリ、Renderサービス、GAS、スプレッドシートを変更しない。
- 一般学習版には氏名入力、採用評価、30分制限、講師への質問評価を持ち込まない。
- 現在、氏名や提出コードの収集・外部送信は行わない。
- 進捗は匿名でブラウザーの `localStorage` にだけ保存する。
- 将来学習記録を導入する場合は、未成年者の利用も考慮して、収集項目・同意・保持期間・削除方法を先に決める。

## 3. プロダクト方針

名称は「モフリスのコードガーデン」。キャラクター名は「モフリス」。

対象はプログラミング未経験のエンジニア志望者。教材を読んでから問題を解くのではなく、ダンジョンを遊んでいたらプログラミングの基礎を習得していた、という体験を目指す。

- 最初の対応言語はPython。
- 主役はコードの手入力。
- PC版Chromeとスマートフォン版Chromeを対象にする。
- 1日5〜15分程度で少しずつ進められる構成にする。
- ストリークを罰にせず、久しぶりでも戻りやすくする。
- 将来はJavaScriptなど他言語を追加できる構造にする。
- Scratch風ブロックは主役にせず、入力補助としてのみ検討する。

基本の学習ループ:

1. ゲーム内で問題に遭遇する。
2. 短いコードを手入力して試す。
3. キャラクターの動きや出力で結果を見る。
4. エラーや失敗理由を手がかりに修正する。
5. 別の状況で同じ知識を再利用する。
6. 章の最後にヒントの少ないボス課題へ挑む。

## 4. 現在実装済みの内容

- VS Code風の画面、コードエディター、行番号
- 一括実行と一行実行
- グリッド型ダンジョンと上下左右スプライト
- 味方・敵MOB、扉、階段、灯
- ふりがなプログラミング形式の関数解説
- ミッション、失敗、再挑戦、クリア演出
- Python学習用の簡易パーサー／実行ロジック
- 端末内の匿名進捗と「続きから冒険する」
- スマートフォン用の「コード／ダンジョン／ミッション」タブ
- スマートフォン用入力補助キー: `Tab`、`()`、`:`、`""`、`=`、改行
- レベル回帰テストと模範解答
- GitHub Pagesによる公開

採用版から撤去済み:

- 氏名入力と記録同意
- 30分タイマー
- 講師・研修・評価文言
- セッションID、氏名、提出コード、所要時間の記録キュー
- GASへの `fetch()` と記録先設定

## 5. 現在の教材

現状は技術ベースとして0〜6階が動く。完成した長期カリキュラムではない。

0. `move()` / `action()` のチュートリアル
1. 命令と実行順序
2. `attack()` / `sayHello()` の使い分け
3. `print()` と文字列
4. `for` と `range()`
5. `input()` と変数
6. 変数、比較、`if / else`

今後の学習候補:

1. コメントとエラー修正
2. 数値と演算
3. 比較と複合条件
4. リスト
5. 辞書
6. 関数、引数、戻り値
7. スコープ
8. クラスとオブジェクト
9. 例外処理
10. ファイル操作
11. JSON
12. API
13. 総合ダンジョン

最初から全章を作らず、まず第1ワールドを3〜4章、合計12〜16ステージ程度へ拡張して遊びやすさを検証する。

## 6. 技術構成

- `index.html`: 画面構造、スマートフォンタブ、入力補助キー
- `styles.css`: メイン画面、エディター、ダンジョン、レスポンシブUI
- `title.css`: タイトル画面
- `answers.css`: モーダルやゲーム補助表示
- `levels.js`: 教材メタデータ、マップ、ミッション、初期コード
- `app.js`: 簡易Python実行、ゲーム状態、描画、画面制御
- `assets/`: 主人公とMOB画像
- `tests/levels.test.cjs`: レベル回帰テスト
- `tests/solutions.cjs`: 模範コード
- `render.yaml`: 将来Renderを使う場合の静的サイト設定

教材データには、`language`、`world`、`chapter`、`minutes` などを持たせている。多言語対応では、今後さらに以下へ分離する。

```text
ゲーム本体
├─ UI・ダンジョン・進捗
├─ 教材データ
│  ├─ Python
│  └─ 将来の言語
└─ 言語エンジン
   ├─ Python風パーサー
   └─ 将来の言語エンジン
```

## 7. 動作確認

```powershell
node tests/levels.test.cjs
node --check app.js
node --check levels.js
```

2026-07-20時点で以下を確認済み。

- 0〜6階の模範解答と当たり判定: 合格
- 6階のMOB全8パターン: 合格
- PC表示: 正常
- 390×844pxのスマートフォン表示: 正常
- スマートフォンの3画面切り替え: 正常
- 入力補助キー: 正常
- ブラウザーコンソールエラー: 0件

## 8. 次に優先する作業

1. 第1ワールドのカリキュラムを3〜4章、12〜16ステージに再設計する。
2. 「写経→穴埋め→エラー修正→ゼロから手入力」と支援を段階的に減らす。
3. レベル一覧／ワールドマップ画面を追加する。
4. 今日の続き、前回の復習、学んだ技を表示する。
5. スマートフォン実機でソフトウェアキーボード表示時の高さを検証する。
6. 教材データ、クリア判定、言語エンジンの依存をさらに分離する。
7. レベル数の固定ループや階層番号依存をなくす。
8. JavaScriptなど第2言語を入れる前に、言語エンジンのインターフェースを設計する。

## 9. 制約と注意点

- 現在のPythonはブラウザー内の簡易パーサーであり、本物のPython処理系ではない。
- 対応構文を増やすほど簡易パーサーの複雑性が上がる。一定段階でPyodide等の採用を比較検討する。
- 静的Webアプリなので、問題、模範解答、判定ロジックは閲覧できる。
- 正解を本当に隠す必要が出た場合だけサーバー側判定を検討する。
- Google Fontsへの外部アクセスがある。プライバシー要件に応じてローカルまたはシステムフォントへ変更する。
- GitHub Pagesは `main` から公開される。反映にはpush後少し時間がかかる場合がある。

## 10. 新しいチャットへ渡す文章

> `Ray0308/code-garden-learning` は一般学習用「モフリスのコードガーデン」の専用リポジトリです。最初に `LEARNING_APP_HANDOFF.md` をすべて読んでください。体験入社用の `Ray0308/code-garden-training`、初期研修版 `Ray0308/code-garden`、既存Render、GAS、スプレッドシートには変更を加えないでください。対象はプログラミング未経験のエンジニア志望者で、PC・スマートフォンのChromeに対応し、毎日5〜15分遊んでいたらPythonの基礎を習得していた、という体験を目指します。まずテストを実行し、現在の実装と次の優先作業を確認してから開発を続けてください。
