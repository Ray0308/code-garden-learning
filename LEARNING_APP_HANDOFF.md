# モフリスのコードガーデン — 開発引き継ぎ書

更新日: 2026-09-15

## 対象と境界

- リポジトリ: https://github.com/Ray0308/code-garden-learning
- 公開先: https://ray0308.github.io/code-garden-learning/
- mainからGitHub Pagesで公開。pushと公開反映は別々に確認する。
- `code-garden-training`、旧`code-garden`、既存GAS・Render・他用途のスプレッドシートは変更しない。
- 氏名・採用評価・時間制限・進捗監視は導入しない。進捗は言語ごとに端末のlocalStorageへ保存。
- 未追跡の既存ファイルやユーザー変更を勝手に削除・コミットしない。

## 目的と完成基準

対象はPC操作はできるがプログラミング未経験の社員。継続的な自己学習を支え、簡単な業務スクリプト・既存コード読解・Java/PHP研修へ進むための基礎を身につける。

共通編はその入口であり、実務スクリプトを単独で完成できる保証ではない。ゲームAPIを使って入力・出力・制御・演算・仮想保存を学ぶ。実際のファイルや外部APIの操作は未実装の言語別実践編で扱う。

「テスト不要」「完全無欠」「全指摘対応済み」はテストの存在だけを根拠に報告しない。指摘・修正・実行した検証を対応付け、未確認事項を明記する。

## 現在の教材

Python・Java・PHP・JavaScriptの4モード。タイトルとゲーム内で選択。各48ステージ（チュートリアル0 + 通常1〜47）。1〜48階ではない。

| 階層 | 主な内容 |
|---|---|
| 0〜3 | move/action・方向転換・順次実行 |
| 4〜7 | 出力・文字列・引用符修正 |
| 8〜11 | 繰り返し・ブロック・回数変更 |
| 12〜15 | attack/sayHello・正面の相手への対応 |
| 16〜17 | input・mob変数・値の出力 |
| 18〜23 | ランダムMOB3体・if/else・反復 |
| 24〜31 | 変数・型・変換・演算・剰余・優先順位 |
| 32〜39 | 比較・論理演算・条件分岐・繰り返す計算 |
| 40〜43 | リスト・添字・要素数・辞書 |
| 44〜47 | 仮想保存／読込・変数名修正・総合課題 |

ランダムMOBはリセット時には未確定で、正面でinputを実行した際に決まる。再挑戦で敵配置を暗記する課題にしない。デバッグ用階層選択は通常URLにも表示。ヒントは写経課題以外で答えそのものを提示しない方針。

## 実行モデルと制約

教材用簡易処理系であり、Python/JVM/PHP/JavaScriptの完全な標準処理系ではない。1行1文、教材範囲の式・固定回数for・if/elseなどに限定する。各言語の全構文・型・例外・スコープの互換性は未保証。

move/action/attack/sayHello/input/save/loadはゲーム専用API。save/loadの仮想データは1回の実行中だけ保持し、再実行で消える。進捗のlocalStorage保存とは別物。

## 構成

- `levels.js`: 0〜23階の教材・マップ
- `courses/python-common.js`: 24〜47階・解答・課題値
- `courses/python-hints.js`: 段階的ヒント
- `courses/language-variant-tools.js`: 各言語の教材変換
- `core/language-registry.js`: コースとエンジン登録
- `engines/python.js`: 式・命令解析、式評価、構文検出
- `engines/brace-adapter.js`: Java/PHP/JSの対応構文の変換
- `app.js`: 実行・表示・クリア・進捗
- `tests/solutions.cjs`: 0〜23階テスト用解答。24〜47階は教材のsolutionが基準
- `tests/browser-audit.cjs`: 実画面／実ランタイム検証

## 検証

```powershell
npm test
node --check app.js
node --check levels.js
node tests/browser-audit.cjs
```

ブラウザーテストにはPlaywright（今回1.62.1）とEdgeが必要。環境変数PW_CHANNELにchromeも指定できる。独立したヘッドレスブラウザーとローカルHTTPサーバーを使い、通常ブラウザーの進捗を変更しない。

- 模範解答・マップ・課題値・4言語命令一致・文言・回帰
- 4言語×48階×2実行方法、18〜23階のMOB全8組み合わせ
- 関数見本・ふりがな・不要関数・課題変数名・6画面サイズの実行ボタン
- 言語別進捗の再読み込み、不正保存データからの復帰

実行結果・残件は `docs/QUALITY_AUDIT_20260915.md` を参照。実機スマホのキーボード、全ブラウザー、支援技術、全手入力コードの検証を代替しない。

## 次の優先順位

1. 品質監査の残件を解消し、指摘と検証証跡を更新する。
2. 実機スマホのキーボード表示・拡大表示・アクセシビリティ確認。
3. 全48階を学習者目線で教材評価し、迷う説明・強すぎるヒントを整理する。
4. 共通編安定後、業務スクリプトへつながる言語別実践編を設計する。

旧引き継ぎ書は `docs/HANDOFF_20260721_ARCHIVE.md` に保存。現在の実装状況として参照しない。
