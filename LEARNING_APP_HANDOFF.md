# Code Garden 一般学習用アプリ 引き継ぎ書

## 1. この引き継ぎの目的

このフォルダは、現在開発中の「体験入社用 Code Garden」を土台にして、別チャットで「一般学習用 Code Garden」を開発するためのスターターです。

最重要の境界は次のとおりです。

- 体験入社用は、元のチャットと `Ray0308/code-garden-training` で今後も開発する。
- 一般学習用は、このコピーを起点に別チャット・別リポジトリ・別Renderサービスで開発する。
- 一般学習用の作業で、体験入社用のリポジトリ、Renderサービス、GAS、スプレッドシートを変更しない。
- このスターターは体験入社用コミット `e5f51b5` を基準に複製した。

## 2. 一般学習用のプロダクト方針

初心者や子どもが、ゲームを進めながらプログラミングを手書きで学べるアプリにする。

- 最初の対応言語はPython。
- 主役はコードの手入力。コマンドボタンやScratch風ブロックは補助機能として後から追加する。
- 将来は他言語にも対応できる構成を目指す。
- ダンジョンを攻略するゲーム性と、段階的な教材設計を両立する。
- キャラクター名は暫定で「フォっくん」。変更可能にしておく。

体験入社用にある「30分」「研修」「行動評価」「講師に質問できるかを見る」といった採用目的は、一般学習用には持ち込まない。

## 3. コピー済みの開発資産

- VS Code風の画面構成とサイドバー
- コードエディター、行番号、実行・一行実行
- グリッド型ダンジョンとキャラクター移動
- 主人公の上下左右スプライト
- 味方・敵MOBの素材
- 関数リファレンスと、押すと中央に開く「ふりがなプログラミング」形式の解説
- ミッション画面、失敗・再挑戦・クリア演出
- Python学習用の簡易パーサー／実行ロジック
- レベル自動テストと模範コード

現在のコピー元には0〜6階層がある。

0. `move()` / `action()` のチュートリアル
1. 基本操作と `action()`
2. `attack()` / `sayHello()`
3. `print()` と文字列
4. `for`
5. `input()`、変数、`print()`
6. 変数と `if / else`

これは完成した一般教材ではなく、動く技術ベースである。一般学習用のカリキュラムは別途再設計する。

## 4. 意図的に切り離したもの

`config.js` の記録先URLは空にしてある。体験入社用GASへ学習者データを誤送信しないためである。

次のものはこのスターターへ入れていない。

- 体験入社用の `google-apps-script.gs`
- 体験入社用GASの公開URL
- Git履歴とGitHub接続情報

学習記録を実装する場合は、一般学習用専用の保存先を新しく作ること。未成年者の利用も想定し、収集する個人情報、同意、保持期間を先に決める。

## 5. 別チャットで最初に行うこと

1. この引き継ぎ書とソース全体を確認する。
2. 一般学習用の名称、対象年齢、1回の学習時間、対応端末をユーザーと決める。
3. 採用・体験入社専用の文言、氏名入力、30分タイマー、記録送信を洗い出して一般学習用へ置き換える。
4. Pythonの学習順序と各章のゲーム課題を設計する。
5. レベル定義を教材データとして分離し、章を追加しやすい構造へ整理する。
6. 新しいGitHubリポジトリとRenderサービスを作る。既存の `code-garden-training` は使わない。
7. 自動テストを保ったまま、最初の一般学習用章を実装する。

学習候補は、出力、コメント、エラー、変数、型、入力、演算子、条件分岐、繰り返し、リスト、関数、クラス、ファイル、APIなど。ただし、一般的な教科書順をそのまま採用せず、ゲームとして触って理解しやすい順に組み直す。

## 6. 技術構成

- `index.html`: 画面構造
- `styles.css`: メイン画面とエディターUI
- `title.css`: タイトル画面
- `answers.css`: テスト用回答表示など
- `app.js`: レベル、簡易Python実行、ゲーム状態、画面制御
- `config.js`: 外部記録先。現在は空
- `assets/`: キャラクターとMOB画像
- `tests/levels.test.cjs`: レベルの回帰テスト
- `tests/solutions.cjs`: テスト用模範コード
- `render.yaml`: 一般学習用として改名済みのRender設定

現状は静的Webアプリであり、配信されたHTML/CSS/JavaScriptは閲覧できる。問題・正解・判定ロジックを本当に隠す必要がある場合は、将来サーバー側へ移す。

## 7. 動作確認

```powershell
node tests/levels.test.cjs
node --check app.js
```

PowerShellの実行ポリシーで `npm.ps1` が止まる環境では、`npm test` ではなく上記の `node` コマンドを使う。

## 8. 別チャットへ最初に送る文章

次の文章と、このフォルダまたはZIPを新しいチャットへ渡す。

> 添付したZIPは、体験入社用Code Gardenをベースに複製した一般学習用スターターです。最初に `LEARNING_APP_HANDOFF.md` をすべて読んでください。一般学習用はこのソースを起点に開発し、元の採用面接用リポジトリ `Ray0308/code-garden-training`、既存Renderサービス、既存GASには一切変更を加えないでください。主役は手書きPythonで、Scratch風ブロックは補助機能です。まず現状を確認し、一般学習用として残す部分・外す部分と、最初のカリキュラム案を提案してください。
