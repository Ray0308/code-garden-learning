# テスト指摘 修正一覧（2026-08-17）

対象: `CODE_DUNGEON_テスト結果記入表_20260813.xlsx`

## 修正内容

| 分類 | 対象 | 指摘 | 対応 |
|---|---|---|---|
| 判定 | 10階層 | `for` を使わず同じ移動を書いても通る | `for` を必須構文として検証 |
| 判定 | 17階層 | 入力値を出力しなくても通る | 言語別の出力構文を必須として検証 |
| 教材 | 8階層 | Pythonのインデント理由が分かりにくい | 半角4スペースと処理ブロックの意味を説明 |
| 教材 | 11階層 | 繰り返しの外に `move()` がある理由が不明 | 波括弧内へ移す修正課題であることを明記 |
| 教材 | 21階層 | 3組すべてが逆で修正量が多い | 1組だけ誤り、残り2組は正しい初期コードへ変更 |
| 教材 | 30階層 | 全言語でクリアできない | `%` の初出を写経形式にし、実行可能なお手本を表示 |
| 関数一覧 | 0・6・8階層 | 未使用の方向転換が表示される | 階層別の表示対象から除外 |
| 関数一覧 | 16〜23階層 | PHP入力例が `$value` | 課題で使う `$mob` に統一 |
| 関数一覧 | 23階層 | `for` の例に処理本体がない | 波括弧内の `move();` まで表示 |
| 関数一覧 | 24〜29階層 | PHP変数例がすべて `$value` | 各課題の変数名を自動参照して表示 |
| 表示 | JavaScript 7階層 | コメントが `print` のまま | 言語変換後の `console.log` を表示 |
| 表示 | Java/PHP/JavaScript 8・11階層 | Python固有のインデント説明 | 波括弧を使う言語向けの説明へ分離 |
| 表示 | 関数一覧 | ふりがなが表示されない | `ruby` / `rt` 要素で読みを描画 |

## 再発防止

- 4言語 × 全48階層について、解答例の構文解析と命令列の一致を自動検証。
- 必須構文（`for`、出力、`%`）を回帰テストへ追加。
- 階層別の関数表示・変数名・言語固有コメントを回帰テストへ追加。
- PC低解像度を含む実ブラウザ操作を公開前に確認。
