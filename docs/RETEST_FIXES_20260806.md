# 再テスト修正一覧（2026-08-06）

対象: Google Drive「修正依頼」フォルダに提出された Python / Java / PHP / JavaScript の再テスト結果

| 区分 | 対象 | 指摘 | 原因 | 修正内容 | 確認 |
|---|---|---|---|---|---|
| 言語変換 | PHP | `print()`の変換後に`echo )`が表示される | `print(`だけを文字置換して閉じかっこを残していた | `print(値)`全体を`echo 値`へ変換 | 自動テスト追加・合格 |
| 教材文言 | Java / PHP / JavaScript | Python固有の`range()`が説明に残る | 構文だけを変換し、トピックと初期コード内コメントを変換していなかった | 各言語のfor文表記へ統一 | 全教材データ走査・合格 |
| 構文解析 | PHP / Java / JavaScript | `else{`など空白の置き方で正しいコードが失敗する | `else {`との完全一致で解析していた | `else`と`{`の間の空白を任意化 | 空白あり・なしの回帰テスト合格 |
| エラー表示 | Java | 不正なfor文が「インデントが多すぎます」になる | 未対応ブロックとして処理後、本文だけを深いインデントとして解釈していた | for文の初期値・条件・更新式を案内する専用エラーを追加 | 誤案内が出ないことをテスト済み |
| クリア条件 | FLOOR 24（三影の門） | forを使わず命令を並べてもクリアできる | 実行結果だけを判定し、学習対象の構文を確認していなかった | `for`必須条件を教材データに追加し、実行前に検証 | forなし失敗・模範解答成功 |
| 課題仕様 | FLOOR 28 / 30 / 34〜48 | 必要な入力値・期待結果が問題文に不足 | 課題判定値はデータにあるが、学習者向け文面に出していなかった | 数値、文字列、判定基準、保存キーを問題文へ明記 | 全48階層の文言監査合格 |
| 初期コード | FLOOR 26 / 28 / 30 / 32 / 34 / 36 / 38 / 40 / 42 / 44 / 46 / 48 | 「変更」「修正」課題なのに変更対象コードがない | 後半課題は壊れたコードがある1件以外、コメントだけを初期表示していた | 各変更・デバッグ課題へ意図的な誤りを含む初期コードを追加 | 4言語すべてでコメントのみがないことを確認 |
| 教材文言 | FLOOR 21 | お手本が表示されないのに「お手本を閉じ」と案内する | Python版の表現を各言語へそのまま流用していた | 「これまでに学んだ構文を自分で書く」へ変更 | 4言語へ反映確認 |
| レイアウト | PC・低い画面 | 下部の実行ボタンを押せない | 学習案内が内容量に応じて伸び、実行欄を押し出す場合があった | 学習案内と展開コードへ最大高・内部スクロールを設定 | CSS回帰条件と静的確認済み |

## テスト結果

- `tests/levels.test.cjs`: 合格
- `tests/language-modes.test.cjs`: 合格
- Python / Java / PHP / JavaScript、各48階層の模範解答解析と動作一致: 合格
- PHP誤変換、Python固有表記、変更・デバッグ用初期コード、for必須条件、Javaのエラー案内: 再発防止テスト追加済み

## 次フェーズ候補

FLOOR 25以降の課題は仕様不明と初期コード不足を今回解消した。ダンジョン形状や操作目的の種類を増やす「ゲーム性の再設計」は、教材の正誤修正とは分けて次フェーズで行う。
