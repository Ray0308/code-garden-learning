# 画像添付テスト指摘 修正一覧（2026-08-31）

対象: `CODE_DUNGEON_テスト結果記入表_20260813.xlsx` の画像添付タブ

| 対象 | 画像で確認した不具合 | 対応 |
|---|---|---|
| PHP 46階層・開始画面 | `$total = price * count` と表示され、`$price`・`$count`・`;` が欠落 | `$total = $price * $count;` に修正 |
| PHP 46階層・クリア画面 | 開始画面と同じ不完全なコード例を表示 | 共通の教材構文データを修正し、クリア画面にも反映 |
| Java / JavaScriptの代入例 | 同じ変換経路で文末記号が欠ける可能性 | `var ...;` / `let ...;` の完全な文へ統一 |
| 17階層・4言語 | 任意の文字列を出力しても出力構文の条件を満たせる | `mob` 自体の出力を必須にし、ダミー出力による迂回を拒否 |

再発防止として、3言語それぞれの46階層で実際に表示される教材構文を完全一致でテストする。

## 文字セル指摘の再監査

- 二宮担当分: 8階層のインデント説明、17階層の`mob`出力、30階層のクリアを再確認。
- 佐久間担当分: 11階層のブロック修正課題、21階層の誤り1か所化、30階層のクリアを再確認。
- 本田担当分: チュートリアル・6・8階層の不要な方向転換非表示、16〜23階層の`$mob`、23階層のfor本体、24〜29階層の課題別変数名を公開画面で再確認。
- 長澤担当分: JavaScript 7階層の`console.log()`、全関数見本のふりがな、10階層のfor必須、11階層の波かっこ説明、17階層の`console.log(mob)`必須、30階層のクリアを再確認。

17階層は従来「任意の出力構文」で通過できたため、`mob`以外のダミー出力を4言語すべてで拒否するテストへ強化した。
